// Code generated by goa v3.22.3, DO NOT EDIT.
//
// calc HTTP client CLI support package
//
// Command:
// $ goa gen calc/design

package cli

import (
	calcc "calc/gen/http/calc/client"
	"flag"
	"fmt"
	"net/http"
	"os"

	goahttp "goa.design/goa/v3/http"
	goa "goa.design/goa/v3/pkg"
)

// UsageCommands returns the set of commands and sub-commands using the format
//
//	command (subcommand1|subcommand2|...)
func UsageCommands() []string {
	return []string{
		"calc (multiply|divide|redirect)",
	}
}

// UsageExamples produces an example of a valid invocation of the CLI tool.
func UsageExamples() string {
	return os.Args[0] + ` calc multiply --a 360622074634248926 --b 8133055152903002499` + "\n" +
		""
}

// ParseEndpoint returns the endpoint and payload as specified on the command
// line.
func ParseEndpoint(
	scheme, host string,
	doer goahttp.Doer,
	enc func(*http.Request) goahttp.Encoder,
	dec func(*http.Response) goahttp.Decoder,
	restore bool,
) (goa.Endpoint, any, error) {
	var (
		calcFlags = flag.NewFlagSet("calc", flag.ContinueOnError)

		calcMultiplyFlags = flag.NewFlagSet("multiply", flag.ExitOnError)
		calcMultiplyAFlag = calcMultiplyFlags.String("a", "REQUIRED", "Left operand")
		calcMultiplyBFlag = calcMultiplyFlags.String("b", "REQUIRED", "Right operand")

		calcDivideFlags = flag.NewFlagSet("divide", flag.ExitOnError)
		calcDivideAFlag = calcDivideFlags.String("a", "REQUIRED", "Left operand")
		calcDivideBFlag = calcDivideFlags.String("b", "REQUIRED", "Right operand")

		calcRedirectFlags = flag.NewFlagSet("redirect", flag.ExitOnError)
	)
	calcFlags.Usage = calcUsage
	calcMultiplyFlags.Usage = calcMultiplyUsage
	calcDivideFlags.Usage = calcDivideUsage
	calcRedirectFlags.Usage = calcRedirectUsage

	if err := flag.CommandLine.Parse(os.Args[1:]); err != nil {
		return nil, nil, err
	}

	if flag.NArg() < 2 { // two non flag args are required: SERVICE and ENDPOINT (aka COMMAND)
		return nil, nil, fmt.Errorf("not enough arguments")
	}

	var (
		svcn string
		svcf *flag.FlagSet
	)
	{
		svcn = flag.Arg(0)
		switch svcn {
		case "calc":
			svcf = calcFlags
		default:
			return nil, nil, fmt.Errorf("unknown service %q", svcn)
		}
	}
	if err := svcf.Parse(flag.Args()[1:]); err != nil {
		return nil, nil, err
	}

	var (
		epn string
		epf *flag.FlagSet
	)
	{
		epn = svcf.Arg(0)
		switch svcn {
		case "calc":
			switch epn {
			case "multiply":
				epf = calcMultiplyFlags

			case "divide":
				epf = calcDivideFlags

			case "redirect":
				epf = calcRedirectFlags

			}

		}
	}
	if epf == nil {
		return nil, nil, fmt.Errorf("unknown %q endpoint %q", svcn, epn)
	}

	// Parse endpoint flags if any
	if svcf.NArg() > 1 {
		if err := epf.Parse(svcf.Args()[1:]); err != nil {
			return nil, nil, err
		}
	}

	var (
		data     any
		endpoint goa.Endpoint
		err      error
	)
	{
		switch svcn {
		case "calc":
			c := calcc.NewClient(scheme, host, doer, enc, dec, restore)
			switch epn {
			case "multiply":
				endpoint = c.Multiply()
				data, err = calcc.BuildMultiplyPayload(*calcMultiplyAFlag, *calcMultiplyBFlag)
			case "divide":
				endpoint = c.Divide()
				data, err = calcc.BuildDividePayload(*calcDivideAFlag, *calcDivideBFlag)
			case "redirect":
				endpoint = c.Redirect()
			}
		}
	}
	if err != nil {
		return nil, nil, err
	}

	return endpoint, data, nil
}

// calcUsage displays the usage of the calc command and its subcommands.
func calcUsage() {
	fmt.Fprintln(os.Stderr, `The calc service performs operations on numbers.`)
	fmt.Fprintf(os.Stderr, "Usage:\n    %s [globalflags] calc COMMAND [flags]\n\n", os.Args[0])
	fmt.Fprintln(os.Stderr, "COMMAND:")
	fmt.Fprintln(os.Stderr, `    multiply: Multiply implements multiply.`)
	fmt.Fprintln(os.Stderr, `    divide: Divide implements divide.`)
	fmt.Fprintln(os.Stderr, `    redirect: Redirect implements redirect.`)
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Additional help:")
	fmt.Fprintf(os.Stderr, "    %s calc COMMAND --help\n", os.Args[0])
}
func calcMultiplyUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calc multiply", os.Args[0])
	fmt.Fprint(os.Stderr, " -a INT")
	fmt.Fprint(os.Stderr, " -b INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Multiply implements multiply.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -a INT: Left operand`)
	fmt.Fprintln(os.Stderr, `    -b INT: Right operand`)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `calc multiply --a 360622074634248926 --b 8133055152903002499`)
}

func calcDivideUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calc divide", os.Args[0])
	fmt.Fprint(os.Stderr, " -a INT")
	fmt.Fprint(os.Stderr, " -b INT")
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Divide implements divide.`)

	// Flags list
	fmt.Fprintln(os.Stderr, `    -a INT: Left operand`)
	fmt.Fprintln(os.Stderr, `    -b INT: Right operand`)

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `calc divide --a 4288748512599820841 --b 4212629202012168060`)
}

func calcRedirectUsage() {
	// Header with flags
	fmt.Fprintf(os.Stderr, "%s [flags] calc redirect", os.Args[0])
	fmt.Fprintln(os.Stderr)

	// Description
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, `Redirect implements redirect.`)

	// Flags list

	// Example block: pass example as parameter to avoid format parsing of % characters
	fmt.Fprintln(os.Stderr)
	fmt.Fprintln(os.Stderr, "Example:")
	fmt.Fprintf(os.Stderr, "    %s %s\n", os.Args[0], `calc redirect`)
}
